# Ask the user for a string and print out
# whether this string is a palindrome or not.
# (A palindrome is a string that reads the same forwards and backwards.)



# define what a palindrome means
def palindrome(word):
    reversed = word[::-1]

# compare them and tell if it is or isn't palindrome
    if reversed == word:
        print("this word is a palindrome")
    else:
        print("its not")

# check the word
palindrome(word=input("Input word:"))
